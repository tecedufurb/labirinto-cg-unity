/// <summary>
/// An enum containing all the types of objects instantiable in the maze.
/// </summary>
public enum ObjectTypes {
    EMPTY = 0,
    WALL = 1,
    START = 2,
    END = 3,
    GROUND = 4
}